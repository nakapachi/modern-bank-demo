spring:
  application:
    name: learning-bank
  datasource:
    url: jdbc:h2:file:./data/learning-bank;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ""
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
server:
  error:
    include-message: never
